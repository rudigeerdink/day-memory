# Day Memory

iOS app (SwiftUI + SwiftData) for tracking work location, travel, and trip evidence by day.

## Open in Xcode

Open `Day Memory.xcodeproj` in this directory.

## Git

This repository was split from a monorepo; history for the `Day Memory` path is preserved in the first commit’s ancestry.
